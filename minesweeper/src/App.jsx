import { useState } from 'react';

function App() {
    const [count, setCount] = useState(0)

    return (
        <div>
          <h1>From the main branch.</h1>
        </div>
    );
};

export default App
